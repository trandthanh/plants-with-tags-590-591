<div class="container">
  <%= simple_form_for [@plant, @plant_tag] do |f| %>
    <%= f.input :tag, collection: @tags, input_html: { multiple: true }, include_hidden: false %>
    <%= f.submit "Add a plant tag", class: "btn btn-primary" %>
  <% end %>
</div>